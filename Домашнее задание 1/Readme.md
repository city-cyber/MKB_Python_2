Задание1
1. Присваиваем значение переменной, задаем туда слово.
2. Вычисляем длину слова. Если длина слова является чётной, то вычисляем среднюю букву и берем две средних буквы.
3. Если длина слова нечётная, то вычисляем среднюю букву и берём одну букву.

Задание2
1.  Проверка на равенство длин:
    Проверяем условие if len(boys) != len(girls):.
    Если количество юношей и девушек не совпадает, выводится сообщение "Внимание, кто-то может остаться без пары!", и дальнейшая логика по созданию пар пропускается.

2.  Сортировка списков:
    Если длины списков равны, мы используем функцию sorted() для создания новых отсортированных списков sorted_boys и sorted_girls.

3.  Создание и вывод пар:
    Выводим заголовок "Идеальные пары:".
    Затем используем цикл for boy, girl in zip(sorted_boys, sorted_girls).
